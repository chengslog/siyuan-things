# siyuan-things 本机部署指南

> 场景：另一台电脑上写好的代码已推送到 GitHub，本文档说明如何在这台电脑上拉取、编译，并更新到思源里生效。
> 最后更新：2026-08-05

---

## 快速流程（4 步）

1. `git pull` 拉代码
2. `npm run build` 编译
3. 把 **dist 目录里的全部内容** 复制到思源插件目录
4. **重启思源**（或在设置里禁用再启用插件）——缺了这步更新不生效！

---

## 路径说明（本机）

| 项目 | 路径 |
|------|------|
| 代码仓库 | `d:\project\siyuan_Things_plugin` |
| 编译产物 | `d:\project\siyuan_Things_plugin\dist` |
| 思源插件目录 | `D:\siyuan\data\plugins\siyuan-things` |

> ⚠️ 思源工作空间所在盘符**因机器而异**：本机是 `D:\siyuan`。如果某台机器是 `C:\siyuan` 或其他位置，把下面命令里的路径换成那台机器的实际路径（即思源工作空间下的 `data\plugins` 目录）。

---

## 详细步骤

### 第 1 步：拉取最新代码

```powershell
cd d:\project\siyuan_Things_plugin
git pull origin main
```

### 第 2 步：安装依赖

```powershell
npm install
```

> package.json 没变时这步几秒就过，每次都执行也无害，可以养成习惯。

### 第 3 步：编译

```powershell
npm run build
```

看到 `✓ built in ...` 即成功，产物在 `dist` 目录：

| 文件 | 作用 |
|------|------|
| `index.js` | 插件主逻辑（编译产物，**更新的核心**） |
| `index.css` | 样式 |
| `kernel.js` | 内核入口 |
| `plugin.json` | 插件清单 |
| `i18n/` | 多语言 |
| `icon.png` / `preview.png` / `README*` / `docs/` | 图标、预览图、说明 |

### 第 4 步：部署——把 dist 内容复制到插件目录

**PowerShell / cmd：**

```powershell
xcopy /E /Y /I "dist\*" "D:\siyuan\data\plugins\siyuan-things\"
```

**Git Bash：**

```bash
cp -rf dist/. /d/siyuan/data/plugins/siyuan-things/
```

> 直接覆盖即可，不需要先清空插件目录、也不用手动删旧文件。

### 第 5 步：重新加载插件 ⚠️ 关键

**必须重启思源，或在思源「设置 → 插件」里把 siyuan-things 禁用再启用。**

思源只在插件加载时读取插件 JS，仅仅关掉再打开 Things 标签页**不会生效**。
（"改了却没变化"最常见的原因就是漏了这步。）

---

## 常见问题

| 现象 | 处理 |
|------|------|
| 更新后没变化 | 没重启思源 / 没禁用再启用插件 |
| 编译报错（找不到模块等） | 先 `npm install` 再 `npm run build` |
| `git pull` 提示本地有改动拉不下来 | 本地改过文件；`git stash` 暂存后 pull，需要的话再 `git stash pop` |
| PowerShell 里连写多条命令 | 用 `;` 分隔，不要用 `&&`（Git Bash 里 `&&` 可以） |
| 想确认更新是否部署上了 | 看 `D:\siyuan\data\plugins\siyuan-things\index.js` 的修改时间，应为刚才的编译时间 |

---

## 一键脚本（可选）

嫌敲命令麻烦，把下面内容保存为项目目录下的 `update.ps1`，以后右键「使用 PowerShell 运行」即可：

```powershell
cd $PSScriptRoot
git pull origin main
npm install
npm run build
xcopy /E /Y /I "dist\*" "D:\siyuan\data\plugins\siyuan-things\"
Write-Host "部署完成！请重启思源（或设置里禁用再启用插件）" -ForegroundColor Green
```
